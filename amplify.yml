version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - 'index.html'
      - 'src/**/*'
      - 'ui-tokens.json'
  cache:
    paths: []
